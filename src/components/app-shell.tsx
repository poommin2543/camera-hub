import Link from "next/link";
import { Camera, LogOut, Shield, Users, Video } from "lucide-react";

export function AppShell({ children, admin = false }: { children: React.ReactNode; admin?: boolean }) {
  return <div className="min-h-dvh bg-slate-950 text-slate-100"><a href="#main" className="sr-only focus:not-sr-only focus:fixed focus:z-50 focus:m-3 focus:rounded focus:bg-white focus:p-3 focus:text-black">ข้ามไปเนื้อหาหลัก</a><header className="sticky top-0 z-20 border-b border-white/10 bg-slate-950/95 backdrop-blur"><div className="mx-auto flex min-h-16 max-w-[1800px] items-center justify-between px-4 sm:px-6"><Link href="/live" className="flex min-h-11 items-center gap-3 font-semibold"><span className="grid size-9 place-items-center rounded-lg bg-blue-600"><Camera size={19} /></span><span>Camera Hub</span></Link><nav aria-label="เมนูหลัก" className="flex items-center gap-1"><Link className="nav-link" href="/live"><Video size={17} /><span className="hidden sm:inline">Live</span></Link>{admin && <><Link className="nav-link" href="/admin/cameras"><Shield size={17} /><span className="hidden sm:inline">กล้อง</span></Link><Link className="nav-link" href="/admin/users"><Users size={17} /><span className="hidden sm:inline">ผู้ใช้</span></Link></>}<Link className="nav-link" href="/api/auth/signout"><LogOut size={17} /><span className="sr-only sm:not-sr-only">ออกจากระบบ</span></Link></nav></div></header><main id="main" className="mx-auto max-w-[1800px] p-4 sm:p-6">{children}</main></div>;
}
